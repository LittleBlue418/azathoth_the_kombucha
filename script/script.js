$(() => {

  load_story(initial)

})

function load_story(state) {
  // Find the div
  // Find the corresponding JSON file
  // Get the correct object by ID
  // Input that text into the text div
  // Input the image address into

}